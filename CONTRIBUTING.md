# コントリビューションガイド

このリポジトリへのコントリビューションを歓迎します。Issue の報告・機能提案・コードの改善など、どの形でも歓迎です。

## クイックスタート

```bash
npm install
npm run dev        # 開発サーバー
npm run build      # ビルド検証
```

## 開発の流れ

1. このリポジトリをフォークし、feature ブランチを作成します
2. 変更を加えます
3. `npm run build` が成功することを確認します
4. Pull Request を作成します

## コーディング規約

- コメントは最小限に（コードの意図はコミットメッセージと PR で伝える）
- 設定値の変更は `src/consts.ts` に集約する
- スクリプトの共通処理は `scripts/lib/common.mjs` に追加する
- frontmatter のスキーマを変える場合は `src/content.config.ts` と合わせて変更する

## コミットメッセージ

日本語で簡潔に、変更内容が分かるように書いてください（例: `ラベル自動作成ワークフローを追加`）。

## Issue / PR について

- バグ報告: 再現手順と期待する動作を記載してください
- 機能提案: ユースケースと背景を記載してください

## テンプレート利用者の皆様へ

このリポジトリをテンプレートとして利用している場合、コンテンツ（`src/content/posts/` の記事）は各自のブログ運用の一部です。リポジトリ本体へのフィードバック（改善・不具合）のみをこのリポジトリの Issue / PR に投稿してください。
